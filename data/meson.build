install_subdir('image', install_dir: datadir / meson.project_name(), strip_directory: true)
install_subdir('doc', install_dir: datadir / meson.project_name(), strip_directory: true)
# Docs shouldn't go here, but I'll save that for when the build system works - right now it's just doing
